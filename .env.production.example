# Production Environment Variables Template
# SECURITY: This file is safe to commit as it contains NO real credentials
# Copy this to .env.production and fill with real values for deployment

# Supabase Configuration (Production)
# Get these from your Supabase project dashboard > Settings > API
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your_production_anon_key_here

# Production Application URL
# Replace with your actual production domain
VITE_APP_URL=https://yourdomain.com

# API Protection (Optional)
# Set to false to disable header sanitization if needed
VITE_API_PROTECTION=true

# DEPLOYMENT NOTES:
# 1. NEVER commit .env.production with real values
# 2. Use platform environment variables instead (Vercel, Netlify, etc.)
# 3. Rotate credentials regularly
# 4. Ensure different credentials for production vs development