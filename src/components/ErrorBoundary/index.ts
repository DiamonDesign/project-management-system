// Export-only file to avoid fast refresh warnings
export {
  ErrorBoundary,
  PageErrorBoundary,
  ComponentErrorBoundary,
  useErrorHandler,
} from './ErrorBoundary';

// Default export for common usage
export { ErrorBoundary as default } from './ErrorBoundary';