# Test Environment Variables Template
# SECURITY: This file is safe to commit as it contains NO real credentials
# Copy this to .env.test and fill with real values for testing

# Supabase Configuration (Test/Staging)
# IMPORTANT: Use DIFFERENT credentials from production
VITE_SUPABASE_URL=https://your-test-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your_test_anon_key_here

# Test Application URL
# Usually localhost for testing
VITE_APP_URL=http://localhost:8080

# API Protection (Optional)
# Can be disabled for testing if needed
VITE_API_PROTECTION=false

# TESTING NOTES:
# 1. Use separate Supabase project for testing
# 2. NEVER use production credentials in tests
# 3. Keep test data isolated from production
# 4. This file should NOT contain real credentials